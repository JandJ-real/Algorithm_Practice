dfs bfs模板(记忆化搜索板子)

字符串相关函数(子串,next数组,匹配,,)()，大小写处理，与其他类型的转换，多行输入，

stl库函数的使用：set，map，priority_queue(寻找前k大元素)(dijstra和prim),unique(返回指向新尾部的迭代器)


c++数据结构：树，图 
树的表示：邻接表
树的算法：树的重心(数据结构依然用图邻接表)，经典dfs遍历

图的表示：邻接表(有向无向带权不带无脑选)，带权图
图的算法：弗洛伊德算法(三重循环dpP2853注释 所有顶点对最短距离(也可用bitset优化) 顶点对可达性)(bitset优化P2853)，dfs，bfs，prim ,克鲁斯卡尔算法(最小生成树)，迪杰斯特拉算法(单源最短距离，不支持负权边) bellmanford算法(单源最短距离，支持负权边) 拓扑排序P1113 Tarjan算法求强连通分量P3387(缩点)

高精度板子

前缀和，差分 板子

四边形不等式优化 板子？

筛法 isprime函数

经典DP板子？